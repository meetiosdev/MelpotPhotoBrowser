import XCTest
@testable import ZLPhotoBrowser

final class ZLPhotoBrowserTests: XCTestCase {
    func testPackageLoads() throws {
        // Basic sanity check that the package compiles and loads
        XCTAssertTrue(true)
    }
}
